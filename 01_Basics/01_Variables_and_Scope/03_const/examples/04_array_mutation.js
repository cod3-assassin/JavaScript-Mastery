const arr = [1, 2, 3];
arr.push(4); // Allowed: mutating array
console.log(arr); // [1, 2, 3, 4]
// arr = [5, 6, 7]; // TypeError

const arrr = ["Apple", "Banana", "Kiwi", "Orange"];

arrr.pop();
console.log(arrr);
